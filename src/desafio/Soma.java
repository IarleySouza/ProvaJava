package desafio;

public class Soma {
    public static void main(String[] args) {
        int x = 0;
        int y = 0;

        int soma = x + y;
        System.out.print( "A soma é: " + soma);
        if (soma != 0) {
            System.out.println("O valor " + soma + " e diferente de zero");
        } else {
            System.out.println("\nA soma e igual a " + soma);
        }
    }
}
